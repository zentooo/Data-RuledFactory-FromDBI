This is Perl module Data::RuledFactory::FromDBI.

INSTALLATION

Type the following command:

    $ curl -L http://cpanmin.us | perl - Data::RuledFactory::FromDBI

Or install cpanm and then run the following command to install
Data::RuledFactory::FromDBI:

    $ cpanm Data::RuledFactory::FromDBI

If you get an archive of this distribution, unpack it and build it
as per the usual:

    $ tar xzf Data-RuledFactory-FromDBI-$version.tar.gz
    $ cd Data-RuledFactory-FromDBI-$version
    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

Data::RuledFactory::FromDBI documentation is available as in POD. So you can do:

    $ perldoc Data::RuledFactory::FromDBI

to read the documentation online with your favorite pager.

LICENSE AND COPYRIGHT

Copyright (c) 2011, zentooo. All rights reserved.

This library is free software; you can redistribute it and/or modify
it under the same terms as Perl itself.
